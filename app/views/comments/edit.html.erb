<h1>Edit a Comment</h1>


<h1>Editing Comment</h1>
<%= form_for :comment, url: comment_path(@commnet.id),
method: :patch do |f| %>
  <% if @comment.errors.any? %>
  <div id="errorExplanation">
    <h2><%= pluralize(@commnet.errors.count, "error") %> prohibited
      this comment from being saved:</h2>
    <ul>
    <% @comment.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
  <% end %>
 
  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>
 
  <p>
    <%= f.submit %>
  </p>
<% end %>
 
<%= link_to 'Back', comment_path %>